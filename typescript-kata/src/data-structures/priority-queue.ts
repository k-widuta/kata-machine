/* 
 * It's the same as heap but in that case we will use priority (not value).
 * So we will store something like this
 * @param {Number} priority
 */

class PriorityQueue {
    public length: number;
    private data: [number, string][]

    constructor() {

    }
}
